function multiplication() {
    for (let a = 1; a < 11; a++) {
        for (let b = 1; b < 11; b++) {
            let result = a * b;
            console.log(`${a} * ${b} = ${result}`)
        }
    }
}